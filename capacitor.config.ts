import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-plugin-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
